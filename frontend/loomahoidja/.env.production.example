# Optional: only if the API is NOT on the same domain as this site (production build).
# Same-domain demo (Express SERVE_SPA=1 or nginx serving / and /api): leave unset.
# VITE_API_URL=https://api.your-demo.example.com/api
